# agent-roster

사용자가 자기 코딩 에이전트(Claude Code)를 브로커에 등록하면, 같은 조직의 에이전트끼리 대화하고, 여러 에이전트를 room에 모아 토론시키는 플랫폼. 브로커는 메시지를 전달하는 통로까지이고, 에이전트의 작업은 관여하지 않는다(제어 경계).

## 상태

계획된 기능 구현 완료. 남은 후속:

- 요청별 인증 분류·적용 — 그때까지 브로커 API(`/send` 등)는 무인증(from 위조 수용 — `docs/architecture.md` §4).

검증 게이트: `bun test` · `bun run typecheck` · `bun run check` — 모든 커밋은 셋 다 통과.

## 문서 규칙

- 용어·엔티티의 기준은 `docs/01-domain-model.md` — 다른 문서는 여기 정의된 이름만 사용 (user/agent/group/user_group/room. 에이전트 정체성은 UUID).
- 구조·확정 결정은 `docs/architecture.md`, 검증된 사실은 `docs/02-tech-notes.md`(검증 수준 표기: 실행 > 원문 > 추정).
- 문서 갱신 시: 같은 내용은 한 곳에만, 대체된 내용은 삭제, 변경 히스토리는 문서에 남기지 않는다(git이 기록).
- `tmp/`는 git 밖 로컬 보관소다 — 코드·문서에서 인용·참조하지 않는다.

## 코드 주석 규칙

- 주석은 기본적으로 두지 않는다 — 동작은 코드·네이밍이, 결정 근거는 `docs/`가, 이력은 git이 말한다.
- 예외는 함정 지점뿐: 테스트로 지킬 수 없는 의도적 결정(코드만 보면 고쳐야 할 것처럼 보이는 값·부재)에만 이유를 자립 한 줄로 단다. 문서 절 번호 인용(`05 §4` 식) 금지.

## 작업 규칙

- **커밋·푸시는 사용자의 명시 지시가 있을 때만.** "이어서 진행"·"계속해"는 커밋 승인이 아니다. 기본은 워킹트리에 두고 상태 보고.
- 이 프로젝트는 claude-peers(claude-channel-peers) 레포와 무관하다 — 코드 계승·하위 호환 의무 없음.
- 문서는 한글로 작성, 한자 사용 금지.
