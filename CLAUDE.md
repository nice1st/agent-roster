# agent-orchestra (가칭)

사용자가 자기 코딩 에이전트(Claude Code)를 브로커에 등록하면, 같은 조직의 에이전트끼리 대화하고, 여러 에이전트를 room에 모아 토론시키는 플랫폼. 브로커는 메시지를 전달하는 통로까지이고, 에이전트의 작업은 관여하지 않는다(제어 경계).

## 상태

**계획 단계 — 코드 없음.** 사용자의 문서 검토가 끝나기 전에는 구현을 시작하지 않는다.

## 문서 규칙

- 용어·엔티티의 기준은 `docs/01-domain-model.md` — 다른 문서는 여기 정의된 이름만 사용 (user/agent/group/membership/room. 에이전트 정체성은 UUID).
- `docs/02-tech-notes.md`는 검증된 사실만 담는다 (검증 수준 표기: 실행 > 원문 > 추정). 설계 결정은 설계 문서가 한다.
- `docs/_archive/`는 읽기 전용 참고자료 — 살아있는 문서에서 링크하지 않는다.
- 문서 갱신 시: 같은 내용은 한 곳에만, 대체된 내용은 삭제, 변경 히스토리는 문서에 남기지 않는다(git이 기록).

## 작업 규칙

- **커밋·푸시는 사용자의 명시 지시가 있을 때만.** "이어서 진행"·"계속해"는 커밋 승인이 아니다. 기본은 워킹트리에 두고 상태 보고.
- 이 프로젝트는 claude-peers(claude-channel-peers) 레포와 무관하다 — 코드 계승·하위 호환 의무 없음. 계승·신규 대비는 `docs/00-vision.md` §5.
- 문서는 한글로 작성, 한자 사용 금지.
- "agent-orchestra"는 가칭 — 이름 확정 시 README·CLAUDE.md·문서 내 지칭을 일괄 갱신한다.
